apply plugin: 'java'

version = 0.1
sourceCompatibility = 1.8

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }

    test {
        java {
            srcDir 'test'
        }
    }
}

jar {
    manifest {
        attributes "Main-Class": "Valid"
    }
}
